# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Process

on:
  workflow_call:
   inputs:
     namess:
       default: 'no name'
       type: string
       required: false

jobs:

  mainworkflow:

    runs-on: ubuntu-latest

    steps:
      # Checkout and setup
      - uses: actions/checkout@v2
        # Grant permission to mvnw
      - name: Check if change is in module1
        uses: dorny/paths-filter@v2
        run: echo i am here da my name is ${{ inputs.namess }}
        id: module1
        with:
          filters: |
            src:
              - 'module1/**'
        # run only if some file in 'src' folder was changed of module 1
      - name: If change is in module1 create release tag
        if: steps.module1.outputs.src == 'true'
        run: |
          echo i am here da my name is ${{ inputs.namess }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}